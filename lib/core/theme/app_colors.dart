import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary green accent
  static const Color primary = Color(0xFF2E7D5E);
  static const Color primaryLight = Color(0xFF4CAF82);
  static const Color primarySurface = Color(0xFFE8F5EF);

  // Backgrounds
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceSecondary = Color(0xFFF2F4F6);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);

  // Emergency / Critical — red only here
  static const Color emergency = Color(0xFFDC2626);
  static const Color emergencySurface = Color(0xFFFEF2F2);
  static const Color emergencyBorder = Color(0xFFFECACA);

  // Border & Divider
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);

  // Shadow
  static const Color shadow = Color(0x0A000000);
}
